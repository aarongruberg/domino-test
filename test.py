# This script is to be called from a Domino Model API, it will take in some value and return that same value.
# This will test calling a python script from an imported git repo in Domino via /repos
def test(val):
  return val
